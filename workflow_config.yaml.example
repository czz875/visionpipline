# 配置已迁移到 tools/cfg/，请改那里。
# 完整工作流配置见 tools/cfg/workflow.example.yaml（可直接复制为 workflow.yaml）。
#
# 项目根的 workflow_config.yaml 现在只是"项目级覆盖"入口——只放项目特定
# 的覆盖项（paths.backup_dir 等），stage 定义全部从 tools/cfg/workflow.yaml
# 继承。
#
# 复制流程：
#   copy tools\cfg\workflow.example.yaml tools\cfg\workflow.yaml
#   notepad tools\cfg\workflow.yaml
#   .conda\python.exe tools\workflow.py --config tools\cfg\workflow.yaml --dry-run
#
# 或保留项目根入口（更省事）：
#   .conda\python.exe tools\workflow.py --config workflow_config.yaml
