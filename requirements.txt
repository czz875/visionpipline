# cjet-vision-pipeline 依赖清单
#
# 安装方法（在项目根目录执行）：
#   .conda\python.exe -m pip install -r requirements.txt
#
# 安装后建议再跑一次冒烟测试：
#   .conda\python.exe -c "import supervision, cleanvision; print(supervision.__version__, cleanvision.__version__)"
# =============================================================================

# ---------- 核心依赖（项目运行时必须） ----------
# 自动标注 / 几何 / 数据集封装都依赖它
supervision
# 模糊目标检测（tools/clean/blurry.py 依赖）
cleanvision

# ---------- 训练 / 推理（推荐） ----------
# YOLOv8 系列训练与推理
ultralytics
# PyTorch（CPU 版本即可，按需替换为 CUDA 版本）
torch
torchvision

# ---------- 图像 / 视频处理 ----------
numpy
opencv-python
Pillow

# ---------- 工作流 / 工具 ----------
# tqdm 进度条（tools/annotate/auto.py 等使用）
tqdm
# 读取 workflow_config.yaml 必需
PyYAML
# COCO 标注转换可选依赖
pycocotools
